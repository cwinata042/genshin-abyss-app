'use client'
import React from 'react'

const AddProductButton = () => {
  return (
    <div>
      <button onClick={() => console.log("Button was clicked!")}>Add to Cart</button>
    </div>
  )
}

export default AddProductButton